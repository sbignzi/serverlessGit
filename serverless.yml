service: serverlessGit

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-1
  

functions:
  generateRandomstring:
    handler: src/handler.generateRandomstring
    timeout: 60 # seconds
    memorySize: 128 # mb